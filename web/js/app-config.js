/**
 * Configuracao do front.
 *
 * API_BASE_URL deve apontar para o backend Spring.
 * Em GitHub Pages, use a URL publica HTTPS da API e configure CORS no backend.
 */
const APP_CONFIG = {
  API_BASE_URL: 'http://localhost:8080',
};
